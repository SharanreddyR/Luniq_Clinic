/**
 * App `colors` — same tokens as **HEALTHAPP** (`healthAppPalette`).
 * Import from here or from `@/constants`.
 */
import { healthAppColors } from '@/constants/healthAppPalette';

export const colors = healthAppColors;

export type AppColors = typeof colors;
