/** Android channel id — must match `app.json` expo-notifications `defaultChannel` and Laravel FCM `channel_id`. */
export const ANDROID_FCM_CHANNEL_ID = 'luniq_clinic_default';

/** Shown in system notification settings and tray grouping for that channel. */
export const ANDROID_NOTIFICATION_CHANNEL_NAME = 'Luniq Clinic';
